{
  "name": "parlasubito-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "clean": "rimraf dist server-dist",
    "build": "npm run clean && tsc && vite build && esbuild server.ts --bundle --platform=node --target=node20 --outfile=server-dist/server.cjs",
    "start": "node server-dist/server.cjs",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}